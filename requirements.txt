# Core deep learning framework
tensorflow==2.17.0

# Numerical & scientific computing
numpy>=1.26.0
scipy>=1.11.0

# Data handling
pandas>=2.0.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# Jupyter & interactive development
jupyterlab>=4.0.0

# Utility packages
tqdm>=4.65.0
protobuf>=4.23.0      # TF uses protobuf; keeping version explicit helps avoid conflicts
h5py>=3.10.0          # for HDF5 model saving
psutil>=5.9.0

# Optional: GPU support (only include if using GPU builds)
# tensorflow[and-cuda]==2.17.0  # for pip’s new TF+CUDA combined package (TF2.16+)
# or if using system-installed CUDA, do not include the above line
